<Window x:Class="AIUsageMonitor.Views.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="About Seth's AI Usage Monitor"
        Width="540"
        Height="430"
        MinWidth="500"
        MinHeight="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#15171D"
        Foreground="#F9FAFB"
        Icon="/Assets/AppIcon.ico"
        ShowInTaskbar="False">
    <Border Padding="22"
            Background="#15171D">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <StackPanel>
                <TextBlock x:Name="TitleTextBlock"
                           FontSize="22"
                           FontWeight="SemiBold"
                           Foreground="#F9FAFB" />

                <TextBlock x:Name="VersionTextBlock"
                           Margin="0,5,0,0"
                           Foreground="#A8AFBA"
                           FontSize="13" />

                <Border Margin="0,18,0,18"
                        Padding="16"
                        Background="#1D2028"
                        BorderBrush="#303641"
                        BorderThickness="1"
                        CornerRadius="8">
                    <StackPanel>
                        <TextBlock Text="Created by Seth A. Robinson"
                                   Foreground="#F3F4F6"
                                   FontSize="16"
                                   FontWeight="SemiBold" />
                        <TextBlock Margin="0,8,0,0"
                                   Text="Windows tray app for checking remaining AI subscription usage across Anthropic, OpenAI/Codex, Antigravity, Gemini, and Cursor."
                                   Foreground="#A8AFBA"
                                   FontSize="13"
                                   TextWrapping="Wrap" />
                        <TextBlock Margin="0,10,0,0"
                                   Text="Developed with significant assistance from AI tools."
                                   Foreground="#8B93A1"
                                   FontSize="12"
                                   TextWrapping="Wrap" />
                    </StackPanel>
                </Border>

                <TextBlock Text="Links"
                           Foreground="#D6DAE1"
                           FontSize="13"
                           FontWeight="SemiBold" />

                <UniformGrid Margin="0,10,0,0"
                             Columns="2"
                             Rows="3">
                    <TextBlock Margin="0,0,12,10"
                               FontSize="13">
                        <Hyperlink NavigateUri="https://github.com/SethRobinson/AIUsageMonitor"
                                   Foreground="#93C5FD"
                                   Click="LinkOnClick">GitHub</Hyperlink>
                    </TextBlock>
                    <TextBlock Margin="0,0,0,10"
                               FontSize="13">
                        <Hyperlink NavigateUri="https://www.rtsoft.com/"
                                   Foreground="#93C5FD"
                                   Click="LinkOnClick">Homepage</Hyperlink>
                    </TextBlock>
                    <TextBlock Margin="0,0,12,10"
                               FontSize="13">
                        <Hyperlink NavigateUri="https://www.codedojo.com/"
                                   Foreground="#93C5FD"
                                   Click="LinkOnClick">Blog</Hyperlink>
                    </TextBlock>
                    <TextBlock Margin="0,0,0,10"
                               FontSize="13">
                        <Hyperlink NavigateUri="https://twitter.com/rtsoft"
                                   Foreground="#93C5FD"
                                   Click="LinkOnClick">Twitter</Hyperlink>
                    </TextBlock>
                    <TextBlock Margin="0,0,12,0"
                               FontSize="13">
                        <Hyperlink NavigateUri="https://bsky.app/profile/rtsoft.com"
                                   Foreground="#93C5FD"
                                   Click="LinkOnClick">Bluesky</Hyperlink>
                    </TextBlock>
                    <TextBlock Margin="0"
                               FontSize="13">
                        <Hyperlink NavigateUri="https://mastodon.gamedev.place/@rtsoft"
                                   Foreground="#93C5FD"
                                   Click="LinkOnClick">Mastodon</Hyperlink>
                    </TextBlock>
                </UniformGrid>
            </StackPanel>

            <StackPanel Grid.Row="1"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right">
                <Button MinWidth="92"
                        Height="34"
                        Padding="14,0"
                        Content="Close"
                        Click="CloseButtonOnClick" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
