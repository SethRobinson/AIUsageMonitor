# AIUsageMonitor — Claude Code instructions

> **ALWAYS read [`AGENTS.md`](AGENTS.md) in the repo root FIRST — before doing anything in this repo** (exploring, planning, editing, or wrapping up a task).
>
> `AGENTS.md` is the source of truth for build, test, diagnostics, screenshot, version-bump, packaging, signing, release, and commit rules. In particular, **a task is not complete until `dotnet build` succeeds and `.\package-release.bat` has finished (or its failure has been reported).** Do not start or finish work without consulting it.

@AGENTS.md
